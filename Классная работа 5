# Задание 1. Базовые операции
# Создайте массив из 1 000 000 случайных целых чисел от 1 до 100:
    # arr = np.random.randint(1, 101, size=1_000_000)
# Найдите:
    # Среднее значение массива
    # Количество чисел больше 50
    # Максимальное значение среди чётных чисел
    # Минимальное значение среди нечётных чисел

import numpy as np 

arr = np.random.randint(1, 101, size = 1_000_000)
# mean_res = np.mean(arr)
# col_res = np.count_nonzero(arr > 50)
# max_res = np.max(arr[arr % 2 == 0])
# min_res = np.min(arr[arr % 2 != 0])

# print('Среднее значение массива',mean_res)
# print('Количество чисел больше 50',col_res)
# print('Максимальное значение среди чётных чисел',max_res)
# print('Минимальное значение среди нечётных чисел',min_res)

# Задание 2. Фильтрация по условиям
# Используя тот же массив, найдите:
    # Количество чисел больше 20 и меньше 40
    # Среднее значение чисел, кратных 5
    # Количество нечётных чисел больше 70
    # Количество чисел, меньших или равных 10


# count_between = np.count_nonzero((arr > 20) & (arr < 40))
# mean_multiple_5 = np.mean(arr[arr % 5 == 0])
# count_odd_high = np.count_nonzero((arr % 2 != 0) & (arr > 70))
# count_large = np.count_nonzero(arr >= 10)

# print("Количество чисел больше 20 и меньше 40:", count_between)
# print("Среднее значение чисел, кратных 5:", mean_multiple_5)
# print("Количество нечётных чисел больше 70:", count_odd_high)
# print("Количество чисел больше или равно 10:", count_large)

#Задание 3. Составные условия
# Найдите:
    # Максимум среди чисел, кратных 7
    # Среднее значение чётных чисел, которые больше 30
    # Количество чисел, которые не делятся на 3
    # Сумму чисел, которые больше 90 или меньше 10

# max_seven = np.max(arr[arr % 7 == 0])
# mean_even_gt_30 = np.mean(arr[(arr % 2 == 0) & (arr > 30)])
# count_not_div3 = np.count_nonzero(arr % 3 != 0)
# sum_extremes = np.sum(arr[(arr > 90) | (arr < 10)])

# print("Максимум среди чисел, кратных 7:", max_seven)
# print("Среднее значение чётных чисел > 30:", mean_even_gt_30)
# print("Количество чисел, не делящихся на 3:", count_not_div3)
# print("Сумма чисел > 90 или < 10:", sum_extremes)
